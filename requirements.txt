# Ядро агента
langgraph>=1.2,<2
langsmith
langchain-core
langchain-text-splitters
pydantic>=2
PyYAML
python-dotenv

# LLM-бэкенды
mlx
mlx-lm
openai        # для OpenRouterProvider (опциональный бэкенд, OpenAI-совместимый API)
# vLLM не входит сюда: он GPU-only и подключается вручную (см. modules/llm/vllm_provider.py)

# Источник данных: живой arXiv вместо утраченных Postgres/LanceDB
requests
beautifulsoup4
pymupdf

# gRPC backend
grpcio
grpcio-tools
protobuf

# Streamlit UI
streamlit

# Оценка качества (faithfulness/coverage/answer_relevancy, сравнение прогонов) —
# отдельный офлайн-харнесс (evaluation/), не часть инференс-пути — см. requirements-eval.txt
