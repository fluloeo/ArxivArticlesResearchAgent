"""Версия схемы JSONL-строк, которые пишет RunWriter — при несовместимом изменении формата
(переименование/удаление поля, а не добавление) увеличивать, чтобы reporting/aggregate.py
мог отличить старые прогоны от новых, а не молча пытаться их сравнивать."""
SCHEMA_VERSION = 1
